# 5-3-3 戦略間相関を考慮した配分最適化システム - 依存関係
# Required packages for the correlation-based allocation optimization system

# Core scientific computing
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0

# Machine learning (optional, for advanced clustering)
scikit-learn>=1.0.0

# Optimization library (optional, for advanced optimization)
cvxpy>=1.2.0

# Visualization
matplotlib>=3.4.0
seaborn>=0.11.0

# Existing system dependencies (should already be installed)
# These are for integration with existing systems
# dataclasses (built-in Python 3.7+)
# typing (built-in Python 3.5+)
# datetime (built-in)
# logging (built-in)
# warnings (built-in)

# Development and testing (optional)
pytest>=6.0.0
jupyter>=1.0.0
